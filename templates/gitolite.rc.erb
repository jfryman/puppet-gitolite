%RC = (
    UMASK                           =>  0027,
    GIT_CONFIG_KEYS                 =>  '',
    LOG_EXTRA                       =>  1,
    ROLES => {
        READERS                     =>  1,
        WRITERS                     =>  1,
    },<% if @grouplist_pgm %>
    GROUPLIST_PGM                   => '<%= @grouplist_pgm %>',<% end %><% if @local_code %>
    LOCAL_CODE                      => '<%= @local_code %>',<% end %>
    ENABLE => [

            'help',
            'desc',
            'info',
            'perms',
            'writable',
            'ssh-authkeys',
            'git-config',
            'daemon',
            'gitweb',
            <%= @repo_specific_hooks ? '' : '#' %>'repo-specific-hooks',
            <%= @wildrepos ? '#' : '' %>'no-create-on-read',
            <%= @wildrepos ? '#' : '' %>'no-auto-create',
    ],
);
