%RC = (
    UMASK                           =>  0027,
    GIT_CONFIG_KEYS                 =>  '',
    LOG_EXTRA                       =>  1,
    ROLES => {
        READERS                     =>  1,
        WRITERS                     =>  1,
    },<% if @grouplist_pgm %>
    GROUPLIST_PGM                   => '<%= @grouplist_pgm %>',<% end %>
    ENABLE => [

            'help',
            'desc',
            'info',
            'perms',
            'writable',
            'ssh-authkeys',
            'git-config',
            'daemon',
            'gitweb',
            <%= @wildrepos ? '' : '\'no-create-on-read\',' %>
            <%= @wildrepos ? '' : '\'no-auto-create\',' %>
    ],
);
